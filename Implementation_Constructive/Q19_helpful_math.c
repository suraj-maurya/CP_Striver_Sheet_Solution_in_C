#include <stdio.h>
#include <string.h>

int main()
{
    char str[100];
    scanf("%100s", str);
    for (int i = 0; i < strlen(str); i++)
    {
        
    printf("%d ",strlen(str));
    }
    


    return 0;
}