// Welcome || 27-01-2024 || 14:39:24
#include <stdio.h>
#include <time.h>

int main() {
    int t, ans=0;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        int x,y,z,a,b,c;
        scanf("%d%d%d", &x,&y,&z);

        if (x>z)a=x;
        else if (z>y)b=z;
        else if()
        


        if(ans)printf("NO\n");
        else{
            printf("YES\n");
            printf("%d %d %d\n",a,b,c);
        }
        
    }
    

    return 0;
}